@using CustomRouting.Pages
@inject IRoutingStateProvider _routingStateProvider;

<Alfattack.Routing.TenantRouter AppAssembly="@typeof(App).Assembly" EveryPage="typeof(Counter)">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Alfattack.Routing.TenantRouter>

@code {
    private async Task HandleNavigation(NavigationContext context)
    {
        
    }
}